/**list skills, rate them (? terrible idea, would rather link to actual things ive done with the skills) */

const skills = [{ name: "HTML/CSS", level: 2, category: "frontend" }];
export const SkillsSection = () => {
  return <section> </section>;
};
